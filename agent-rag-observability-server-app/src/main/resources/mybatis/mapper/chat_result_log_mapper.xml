<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.chyuan.ai.observability.infrastructure.dao.mapper.ChatResultLogMapper">
    <insert id="insert" parameterType="cn.chyuan.ai.observability.infrastructure.dao.po.ChatResultLogPO">
        INSERT INTO chat_result_log (trace_id, source_service, tenant_id, owner_user_id, session_id,
            agent_id, question, answer, prompt_tokens, completion_tokens, total_cost_time_ms,
            final_status, model_version, create_time)
        VALUES (#{traceId}, #{sourceService}, #{tenantId}, #{ownerUserId}, #{sessionId},
            #{agentId}, #{question}, #{answer}, #{promptTokens}, #{completionTokens}, #{totalCostTimeMs},
            #{finalStatus}, #{modelVersion}, #{createTime})
    </insert>
</mapper>
